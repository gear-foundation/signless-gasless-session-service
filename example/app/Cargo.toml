[package]
name = "sessions-app"
version.workspace = true
edition.workspace = true

[dependencies]
sails-rs = { version = "0.4.0", features = ["debug"] }
session-service = { path = "../../session-service" }